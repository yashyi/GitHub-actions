name: hello-world
on: push
jobs:
  hello-world-job:
    runs-on: self-hosted
    steps:
      - name: check out repository code
        uses : actions/checkout@v4
      - run: echo "$(cat hello-world.txt"